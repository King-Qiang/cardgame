<view class="dock">
  <view
    wx:for="{{tabs}}"
    wx:key="id"
    class="dock-item {{active === item.id ? 'active' : ''}}"
    data-path="{{item.path}}"
    data-id="{{item.id}}"
    bindtap="onTap"
  >
    <text class="icon">{{item.icon}}</text>
  </view>
</view>
